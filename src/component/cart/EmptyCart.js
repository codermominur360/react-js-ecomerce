import React from 'react';

const EmptyCart = () => {
    return (
        <div>
            <h1 style={{color:"white", background:"#05186b",fontFamily:"arial"}}> Cart have't Product yet now !</h1>
        </div>
    );
};

export default EmptyCart;